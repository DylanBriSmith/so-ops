# Session Notes — 2026-06-26

## What was done

### OpenRouter LLM provider
- Added `src/so_ops/clients/openrouter.py` — stdlib urllib, same `generate()` interface as OllamaClient
- Added `src/so_ops/clients/base.py` — `LLMClient` Protocol for structural typing
- Updated `src/so_ops/clients/__init__.py` — `make_llm_client(cfg)` factory selects Ollama or OpenRouter
- Updated `config.py` — `OpenRouterConfig` dataclass, `llm_provider` field on `Config`, `ollama` now Optional
- Updated `triage.py`, `health.py`, `vulnscan.py` — use factory instead of hardcoded OllamaClient
- Updated `cli.py` — fixed crash when `ollama` is None in OpenRouter mode
- Updated `config.example.toml` — `llm_provider = "ollama"` at top, commented `[openrouter]` block

### Tests
- `tests/test_openrouter.py` — 22 unit tests, fully mocked
- `tests/test_openrouter_live.py` — 5 live integration tests (require `SO_OPS_OR_API_KEY`, skip otherwise)
- `tests/test_config.py` — updated stale assertion, added openrouter field assertions

### Teams notification provider
- `src/so_ops/clients/notify.py` — added `_send_teams()` sending Adaptive Cards via Power Automate HTTP webhook
- Payload matches the existing Teams webhook format used at Canadian Bearings
- Registered in `PROVIDERS` dispatcher as `"teams"`
- Config key: `[notifications.teams]` with `url` pointing to the Power Automate trigger

### Mock triage script
- `scripts/mock_triage.py` — full end-to-end pipeline test without Elasticsearch
- 9 fake alerts covering auto-noise, LOW, MEDIUM, HIGH (SSH scan + Cobalt Strike)
- Calls real OpenRouter API and sends real ntfy/Teams notifications

### Infrastructure setup
- Forked `benolenick/so-ops` → `DylanBriSmith/so-ops`
- Opened PR against upstream
- Installed so-ops in editable mode, added Python Scripts dir to user PATH

## Security Onion connection

- **Manager:** `192.168.1.231` (HOSVRSOMGR.cdnbrg.com), SO version 3.0
- **Elasticsearch:** `https://192.168.1.231:9200`
- **Firewall:** `sudo so-firewall includehost elasticsearch_rest <ip> && sudo so-firewall apply`
- **ES user:** `soc_viewer` (SO web UI account, read access to alert indices)
- **Indices confirmed accessible:**
  - `logs-suricata.alerts-so` — 174,590 docs
  - `logs-zeek-so` — 81,781,998 docs
  - `logs-detections.alerts-so` — 16 docs

Note: `soc_viewer` gets 403 on `_cluster/health` — this is fine, so-ops doesn't need it.

## Current config state (config.toml — gitignored)

| Setting | Value |
|---|---|
| `llm_provider` | `openrouter` |
| `openrouter.model` | `anthropic/claude-haiku-4-5` |
| ES host | `https://192.168.1.231:9200` |
| ES user | `soc_viewer` |
| ES password | via `SO_OPS_ES_PASSWORD` env var |
| `notifications.teams.enabled` | `false` (live channel — enable carefully) |
| `notifications.ntfy.enabled` | `false` |
| `triage.lookback_hours` | `24` |
| `triage.max_alerts_per_run` | `500` |

## Known issues / next steps

- **Triage is slow** — 194 LLM groups × ~2s each ≈ 6-10 min. Fix options:
  1. Parallel LLM calls with `ThreadPoolExecutor` (biggest win, needs code change)
  2. Schedule triage hourly so each run is small
  3. Expand `triage.auto_noise.signatures` with confirmed-noise rules from the JSONL log

- **Auto-noise list is thin** — only 4 signatures. The LLM is spending calls confirming NTLM, SMB Info, etc. as NOISE every run. Pull `~/so-ops-data/logs/triage.jsonl`, find all `"method":"llm","verdict":"NOISE"` entries, promote those signatures to auto-noise in config.

- **OpenRouter key** — the key in use is the test key from initial setup. 401 errors mid-run indicate it hit a credit limit. Monitor usage on the OpenRouter dashboard.

- **Teams webhook** — `[notifications.teams]` is configured but disabled. When enabling: test first with `so-ops test-notify`. The webhook posts to a live shared Teams channel.

- **ntfy** — topic `so-ops-test` is configured and was tested successfully. Disabled for now.

## How to run

```powershell
# Set env vars (each new session)
$env:SO_OPS_ES_PASSWORD = '123456'
$env:SO_OPS_OR_API_KEY  = 'sk-or-v1-...'
$env:SO_OPS_CONFIG      = 'C:\Users\dnsh\so-ops\config.toml'

# Run triage
so-ops triage

# Reset cursor to re-process last 24h
Remove-Item "$env:USERPROFILE\so-ops-data\state\triage.json" -Force
```

## Git remotes

| Remote | URL |
|---|---|
| `fork` | `https://github.com/DylanBriSmith/so-ops` (push here) |
| `origin` | `https://github.com/benolenick/so-ops` (upstream, PR target) |
